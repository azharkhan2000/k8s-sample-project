apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: db-credentials  #Type Any Name that you want to give for secets creation inside cluster
spec:
  provider: aws
  parameters:
    objects: |
        - objectName: "db-credentials"
          objectType: "secretsmanager"